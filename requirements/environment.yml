prefix: ./env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - jupyterlab
  - geopandas
  - matplotlib
  - descartes
  - networkx
  - pip
  - pip:
    - hydra-core
    - wandb
    - -r file:dev-requirements.txt
